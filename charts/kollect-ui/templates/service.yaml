apiVersion: v1
kind: Service
metadata:
  name: {{ include "kollect-ui.fullname" . }}
  labels:
    {{- include "kollect-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kollect-ui.selectorLabels" . | nindent 4 }}
