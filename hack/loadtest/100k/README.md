# 100k load test bundle (manual cloud gate)

**Status: PLANNED — not executed in CI.**

Generate workload + sharded inventories:

```bash
./generate.sh --namespaces 50 --deployments-per-ns 2000
```

See [docs/operator-manual/load-test-runbook.md](../../../docs/operator-manual/load-test-runbook.md).
